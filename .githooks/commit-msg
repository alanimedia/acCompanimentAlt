#!/bin/sh
# Reject Cursor co-author trailers on commits.
if grep -qiE '^Co-authored-by:.*cursor' "$1"; then
  echo "ERROR: Remove 'Co-authored-by: Cursor' from the commit message." >&2
  echo "This repository must not attribute Cursor on commits or releases." >&2
  exit 1
fi
